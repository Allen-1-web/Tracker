# Shared upstreams (included once). Bot uses network_mode: host on the VPS.

map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

upstream tracker_frontend {
  server frontend:3000;
  keepalive 32;
}

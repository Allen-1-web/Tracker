# Copy to repository root as `.env` (gitignored) for docker compose build-args.
# Values must match frontend/.env.production where overlapping.

SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
TELEGRAM_BOT_USERNAME=my_tracker_bot
